/**
 * ProjectTracker Web Configuration
 * 
 * Bu dosyayı config.js olarak kopyalayıp kendi ayarlarınızı yapın.
 * config.js dosyası .gitignore'da olduğundan git'e eklenmez.
 */

const CONFIG = {
    // API Base URL - Backend API endpoint'iniz
    // Örnek: 'https://api.yoursite.com' veya 'http://localhost:5000'
    API_BASE_URL: 'https://api.yoursite.com',
    
    // Uygulama indirme linki
    DOWNLOAD_URL: 'https://github.com/your-username/projecttracker/releases/latest',
    
    // Demo modu - true ise gerçek API çağrısı yapmaz, false yaparak production'da kullanın
    DEMO_MODE: false
};
